
    // Función para saludar
    function saludar() {
      const nombre = prompt("¡Hola! ¿Cuál es tu nombre?");
      const saludo = `¡Bienvenido, ${nombre}! Esperamos que disfrutes tu visita, este articulo es elaborado por PEDRO FIGUERA.
     si necesitas informacion adicional,visita link al final de cada parrafo.
     `;
      alert(saludo);
    }
